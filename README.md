# claudash

Claude Code plugin marketplace for Dash Platform developers.

## Overview

Provides skills with hybrid documentation: committed keyword lookup tables
(the "lexicon") generated by an indexer skill, which other skills grep then
selectively web-fetch for details. Minimizes token usage, stays current.

## Installation

```bash
# Install from this repo
claude plugin install /path/to/claudash

# Or load for a single session
claude --plugin-dir /path/to/claudash
```

## Skills

| Skill | Triggers | Purpose |
|-------|----------|---------|
| `dash-platform` | Data contracts, Rust SDK, JS/TS SDK, identities, documents | Full Dash Platform development guidance |
| `update-lexicon` | Manual invocation | Crawl repos and docs, regenerate `lexicon/*.md` |

### Usage

Skills activate automatically based on context. To manually invoke:

```
/dash-platform:update-lexicon      # Refresh lexicon files
```

### Project Setup

When building or working in the `dashpay/platform` repo, skills will instruct
you to run the environment setup:

```bash
bash scripts/setup-ai-agent-environment.sh
```

## Lexicon

Files in `lexicon/` contain condensed keyword lookup tables optimized for grep.
Generated by `update-lexicon` — do not edit manually.

| File | Content |
|------|---------|
| `lexicon/contract.md` | Data contract types, JSON Schema, DPP |
| `lexicon/rust.md` | Rust SDK types, functions, patterns |
| `lexicon/js.md` | JS SDK types, functions, patterns |
| `lexicon/grpc.md` | gRPC services, messages, endpoints |

### Link Prefix Conventions

Lexicon tables use abbreviated prefixes to save tokens:

| Prefix | Expands to |
|--------|-----------|
| `P:` | `https://github.com/dashpay/platform/blob/master/packages/` |
| `R:` | `https://dashpay.github.io/platform/api/rust/` |
| `G:` | `https://dashpay.github.io/platform/api/grpc/` |
| `B:` | `https://dashpay.github.io/platform/` |
| `T:` | `https://github.com/dashpay/dash-evo-tool/blob/master/` |
| `Y:` | `https://github.com/PastaPastaPasta/yappr/blob/master/` |
| `D:` | `https://github.com/PastaPastaPasta/dash-bridge/blob/master/` |
| `E:` | `https://github.com/dashpay/evo-sdk-website/blob/master/` |

Skills expand these prefixes when fetching details via WebFetch.

## Marketplace

This repo serves as a marketplace shipping the **dash-platform** plugin.

Claudash's best friend is **[claudius](https://github.com/lklimek/claudius)** — an opinionated dev lifecycle toolkit with agents and skills. Cool people use them together.

See `.claude-plugin/marketplace.json` for details.

## Development

### Validate the plugin

```bash
claude plugin validate .
```

### Test locally

```bash
claude --plugin-dir .
```

### Refresh lexicon

```
/dash-platform:update-lexicon
```

## Architecture Notes

The plugin is designed with future portability in mind:

- **Skill content is platform-agnostic** — markdown bodies work as instructions for any LLM
- **Frontmatter is platform-specific** — Claude Code wiring in YAML headers
- **Lexicon files are portable** — plain markdown tables consumable by any system
- **Link prefixes are conventions** — documented in skill bodies, not platform-tied

## License

GPL-3.0. See [LICENSE](LICENSE).

<sub>🤖 Co-authored by [Claudius the Magnificent](https://github.com/lklimek/claudius) AI Agent</sub>
